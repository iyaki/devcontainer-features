{
    "name": "PHP Installer for Extensions (PIE)",
    "id": "pie",
    "version": "1.0.1",
    "description": "Installs PIE — the official PHP Installer for Extensions (replaces PECL) — from the php/pie GitHub Releases as a self-contained static binary.",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "description": "PIE release version to install (for example: 1.4.9). Use 'latest' for the newest release. Release tags are bare numbers (no 'v' prefix)."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers-extra/features/gh-release"
    ]
}
