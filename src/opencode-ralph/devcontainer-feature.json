{
    "name": "Ralph Agentic Loop runner for OpenCode",
    "id": "opencode-ralph",
    "version": "1.1.0",
    "description": "POSIX-compliant AI Agentic Loop shell runner aimed for spec-driven development workflows.",
    "options": {
        "version": {
            "type": "string",
            "default": "v1.0.1",
            "description": "Release version to install. Use 'latest' or a release tag such as 'v1.0.0'. By now default is v1.0.1 for backward compatibility."
        },
        "installDir": {
            "type": "string",
            "default": "/usr/local/bin",
            "description": "Directory to install the ralph executable."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers-extra/features/opencode"
    ]
}
